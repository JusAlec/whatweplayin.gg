{
  "name": "@gno/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint via root eslint'"
  },
  "dependencies": {
    "@gno/recommender": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240403.0",
    "wrangler": "^3.50.0",
    "vitest": "1.3.0",
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "miniflare": "^3.20240404.0",
    "typescript": "^5.4.0"
  }
}
